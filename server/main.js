import { Meteor } from 'meteor/meteor';

import '../imports/api/menu.js';
import '../imports/api/resto.js';
import '../imports/api/order.js';

Meteor.startup(() => {
  // code to run on server at startup
});
